export default class Item {
  constructor(name, modifier, description, img, move) {
    this.name = name
    this.modifier = modifier
    this.description = description
    this.img = img
    this.move = move
  }
}